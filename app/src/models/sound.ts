export class Sound {
  /**
   *
   * @param id identificador único
   * @param path caminho da música
   * @param icon nome do ícone
   * @param name nome do som
   */
  constructor(public id: string, public path: string, public icon: string, public name) {

  }
}
